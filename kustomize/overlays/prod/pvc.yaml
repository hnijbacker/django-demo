kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-sqldata
spec:
  storageClassName: local-path
  volumeName: pv-sqldata-prod
  accessModes:
  -  ReadWriteOnce
  resources:
    requests:
      storage: 20Mi
